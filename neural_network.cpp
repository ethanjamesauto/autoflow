#include "neural_network.h"

void gradientMult(const Tensor& one, const Tensor& two, Tensor& out) {
    
}

NeuralNetwork::NeuralNetwork(std::vector<Operation> sequence) {
    this->sequence = sequence;
}
